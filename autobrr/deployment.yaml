apiVersion: apps/v1
kind: Deployment
metadata:
  name: autobrr
  namespace: autobrr
spec:
  replicas: 1
  selector:
    matchLabels:
      run: autobrr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        run: autobrr
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: env-vars
        image: ghcr.io/autobrr/autobrr:v1.77.1
        name: autobrr
        ports:
        - containerPort: 7474
        volumeMounts:
        - mountPath: /etc/localtime
          name: tz
          readOnly: true
        - mountPath: /config
          name: autobrr-vol
        resources:
          requests:
            cpu: 10m
            memory: 64Mi
          limits:
            cpu: 250m
            memory: 256Mi
        livenessProbe:
          tcpSocket:
            port: 7474
          initialDelaySeconds: 60
          periodSeconds: 20
        readinessProbe:
          tcpSocket:
            port: 7474
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - hostPath:
          path: /etc/localtime
        name: tz
      - name: autobrr-vol
        persistentVolumeClaim:
          claimName: autobrr-pvc
