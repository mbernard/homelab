configMapGenerator:
- envs:
  - env_vars.properties
  name: env-vars
- files:
  - vpn-wireguard.conf
  name: wg0.conf
kind: Kustomization

namespace: qbittorrent

resources:
- deployment.yaml
- service.yaml
- storage.yaml
